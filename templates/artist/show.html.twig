{% extends 'base.html.twig' %}

{% block title %}Artist{% endblock %}

{% block body %}
    <h1>Artist</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artist.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ artist.name }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ artist.title }}</td>
            </tr>
            <tr>
                <th>Story</th>
                <td>{{ artist.story }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ artist.age }}</td>
            </tr>
            <tr>
                <th>Height</th>
                <td>{{ artist.height }}</td>
            </tr>
            <tr>
                <th>Origin</th>
                <td>{{ artist.origin }}</td>
            </tr>
            <tr>
                <th>Particularity</th>
                <td>{{ artist.particularity }}</td>
            </tr>
            <tr>
                <th>Incredible</th>
                <td>{{ artist.incredible }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('artist_index') }}">back to list</a>

    <a href="{{ path('artist_edit', {'id': artist.id}) }}">edit</a>

    {{ include('artist/_delete_form.html.twig') }}
{% endblock %}
